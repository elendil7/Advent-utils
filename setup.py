from setuptools import find_packages, setup

setup(
    name='adventutils',
    packages=find_packages(include=['src']),
    version='0.1.1',
    description='a helper library for the advent of code',
    author='Thive-N',
    license='MIT',
    install_requires=[],

)
